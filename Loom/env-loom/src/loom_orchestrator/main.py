from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "Orchestrateur pas encore implémenté — cf. backlog T2.3 (AudioProcessor WLK embarqué, "
        "politique de commit, registre de voix — cf. ADR-0039, plus de WebSocket)."
    )


if __name__ == "__main__":
    main()
